1. Test fastapi==0.118.0 + faststream==0.5.48: `docker compose build && docker compose up -d`
2. Update deps: `uv sync --upgrade`
3. Test fastapi==0.124.4 + faststream==0.6.4: `docker compose build && docker compose up -d`
